package com.metaforge.metamodel.api.enums;

/**
 * 实体类型枚举，与 ElementType 值一致，用于领域层实体类型标识。
 */
public enum EntityType {

    BUNDLE,
    BUNDLE_VERSION,
    PACKAGE,
    ENTITY_SCHEMA,
    RELATION_SCHEMA,
    ATTRIBUTE_TEMPLATE
}
